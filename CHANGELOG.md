# Changelog

Все заметные изменения Beacon. Формат — [Keep a Changelog](https://keepachangelog.com/),
версионирование — [SemVer](https://semver.org/). Политика бампа: `docs/versioning.md`.

Версия — единый источник: `package.json` (`manifest.config.ts` читает `pkg.version`).
До `1.0.0`: каждая **новая фича/модуль** (live-verified) = MINOR `0.x.0`; багфикс/UX/honesty/
рефактор/debt = PATCH `0.x.y`. `1.0.0` = весь план Todoist закрыт, нет заглушек/хардкода,
грабли учтены, E2E зелёный.

## [Unreleased]

## [0.8.3] — 2026-07-09

### Fixed
- **Коннекты + Просмотры молча ничего не делали, хотя поле поиска выглядело заполненным.**
  Карточка «Модули» показывала prefill ключевых слов из экспертизы, но **не сохраняла** его
  (`v-model` пишет только по `@change`) → прогон читал пустой `connect:settings.searchKeywords`
  → `no_keywords` → оба модуля no-op. `ensureSearchKeywords` персистит prefill при открытии
  карточки (регионы сохраняются); прогон по-прежнему читает только сохранённое — реально не
  настроенный модуль остаётся безопасным no-op с честным «не задан поиск» (без авто-коннекта
  по выдуманному дефолту, safety-first).
- **Идеи для постов не извлекались в прогоне (мёртвый catch-up).** Регрессия с `c4981e5`
  (28 июня): run-end извлечение идей было под гейтом `autopilotRunning`, который `endRun()`
  зануляет ДО `finally` → catch-up никогда не срабатывал; идеи шли только mid-run путём при
  ≥25 уникальных постах (`IDEA_TARGET`), редко достижимо. Введён loop-local `extractAtEnd`
  (ставится синхронно на естественном конце: feed_exhausted / budget / risk), user-STOP
  пропускается (без повторного зажигания оверлея). Live-verified через CDP (флип `ideas:lastRun`
  на сегодняшнюю дату после feed-exhausted прогона).

## [0.8.2] — 2026-07-03

### Fixed
- **«Сгенерировать идеи» на ленте выдавало «Открой вкладку ленты» (0 постов).** Регрессия
  (с 28 июня): `harvestByScrolling` содержал `if (!autopilotRunning) break` — добавлен, чтобы
  STOP прерывал скролл во время прогона, но он ломал **автономный** harvest (ручная кнопка,
  когда автопилот НЕ запущен → `autopilotRunning=false` → цикл рвался на round 0 → 0 постов →
  `no_feed`). Цикл вынесен в тестируемый `lib/feed/scrollHarvest` с явным `shouldAbort` (дефолт —
  не прерывать); цикл автопилота передаёт `() => !autopilotRunning` (STOP по-прежнему прерывает),
  ручной путь использует дефолт. Live-verified: harvest снова собирает посты.

## [0.8.1] — 2026-07-03

### Fixed
- **Провайдер Gemini переставал генерировать (идеи/комменты), OpenRouter работал.** Причина
  (заземлено на реальном аккаунте): прямой Gemini free-tier = **5 запросов/мин** + периодический
  `503` overload; один прогон бьёт больше вызовов подряд → `429 RESOURCE_EXHAUSTED`/`503` → шаг
  молча падал (OpenRouter не страдает — пул). Добавлен **retry с backoff** для транзиентных ошибок
  (429/5xx), уважающий server-advised `retryDelay` (Gemini «retry in Xs»), с капом (30с) и **честной
  ошибкой** при реальном исчерпании квоты вместо тишины. `RetryingLlmProvider` оборачивает все
  провайдеры (`createLlmProvider`); `FetchHttpClient` бросает типизированный `HttpError{status,
  retryAfterMs}`. Изолированные операции (одна генерация) теперь надёжны; полноценный прогон на
  free-tier gemini-2.5-flash всё равно упирается в 5/мин (лимит Google) — для прогонов лучше модель
  с бóльшим RPM (2.0-flash-lite / 2.5-flash-lite) или OpenRouter.

## [0.8.0] — 2026-07-02

### Added
- **Просмотры профиля (входящие) на дашборде.** Метрика «кто смотрел ваш профиль» (WVMP,
  скользящее окно 90 дней) — прямой outcome pull-петли: профиль стал привлекательнее → тебя
  чаще смотрят. Дневные снапшоты (`ProfileViewsRepository` + generic `dailyHistory`), скалярный
  агрегатор (`computeViewsProgress`: было→стало) и виджет `ProfileViewsTrend.vue` рядом с трендом
  SSI. Источник как у SSI: **internal API primary** (SDUI `sduiid=WvmpAnalytics` POST из service
  worker, csrf + credentials — фоново, без навигации) **+ DOM fallback**; один парсер, две стратегии.
  Честность: окно скользящее → спад показывается **нейтрально**, без тревожно-красного. Имена
  вьюеров (Premium) не нужны и не читаются. **Live-verified** на реальном аккаунте (45 просмотров).
- Движок снапшотов вынесен в переиспользуемый generic (`history/dailyHistory` +
  `SnapshotRefreshService<T>`), общий для SSI и profileViews (DRY); SSI-путь не тронут.

### Note
- Заменяет отменённый эпик «Реальные Входящие» (инбокс-парсинг переписки не делаем: нативный
  LinkedIn-мессенджер + бан-риск). Меряем **видимость**, а не переписку.
- Осознанно **вне V1**: (1) «search appearances» (тот же эндпоинт отдаёт, но headline-метрика —
  просмотры; добавим при желании); (2) DOM-fallback ридер (вырезан как dead code — SDUI POST
  единственный источник, честный throw при drift; можно вернуть как opportunistic-парс, когда
  открыта страница аналитики).

## [0.7.0] — 2026-07-02

### Added
- **SSI-история + виджет тренда на дашборде.** Каждый снапшот SSI бакетится по календарному дню
  (`ssiHistory`), агрегатор считает прогресс по каждому из 4 столпов (`ssiProgress`: from→to→delta),
  а на Dash — sparkline динамики общего балла (`SsiTrend.vue` + чистая view-математика
  `ssiTrendView`). Видно, растёт ли SSI и какой пиллар тянет. (Влито из `feature/ssi-history-stats`.)

## [0.6.3] — 2026-06-30

### Changed
- **Комментарий больше не обязан быть вопросом.** Промпт форсил «ONE clarifying QUESTION»
  (игнорируя даже настройку тона). Теперь — короткая реакция на пост: **краткое мнение/тейк
  ИЛИ уточняющий вопрос**, что уместнее. Тон (expert/friendly/question) задаёт голос, а не
  жёстко форму. Анти-слоп и лимит длины сохранены.

## [0.6.2] — 2026-06-30

### Fixed
- **Все комментарии за прогон уходили под ОДИН пост.** `executeComment` искал редактор глобальным
  `document.querySelector` → брал первый открытый композер на странице, поэтому каждый следующий
  коммент печатался в него же. Редактор рендерится ВНУТРИ узла поста (verified live) → новый
  `findPostEditor(root, urn)` скоупит редактор к нужному посту (пере-находит пост каждый poll —
  устойчиво к ре-рендеру). Теперь каждый коммент идёт под свой пост.
- **Комментарии слишком длинные.** Промпт ужесточён (жёсткий лимит: ≤2 коротких предложения,
  <200 симв., только сам вопрос) + детерминированный `clampComment` обрезает ответ модели до
  первых 2 предложений (модели регулярно переливают промптовый лимит; clamp гарантирует).

## [0.6.1] — 2026-06-29

### Fixed
- **Само-вовлечение: бот лайкал и комментил (×3) СОБСТВЕННЫЙ пост.** Авто-опубликованный пост висел
  вверху ленты, а `LikeFilter` не знал владельца. Теперь `readOwnerName` читает владельца из ленты
  (vanity = первый `/in/` self-card → имя из непустого `alt` аватара, verified live), и движок
  пропускает посты, где автор = владелец (`own_post`). Матч по ИМЕНИ иммунен к churn componentkey —
  причине тройного коммента. При неудаче опознания — fail-open + warn.
- **Просмотр профилей делал 0–3 из 40 после серии коннектов.** Views переиспользовал connect-харвестер
  (только кнопка «Invite to connect»), поэтому уже приглашённые («Pending») были невидимы → harvest 0 →
  пагинация не стартовала. Новый `harvestProfiles` берёт людей по member-componentkey (connect + pending),
  тот же numeric memberId (seen-set валиден). Live: 3 → **38 просмотров**, 35 новых уникальных профилей.

### Changed
- **Просмотры листают страницы вглубь до капа СВЕЖИХ.** `harvestPeoplePaginated` получил `isFresh`-предикат:
  считает невиденных к таргету и листает (до 20 страниц), пока не наберёт `cap` непросмотренных или поиск
  не кончится. Честный reason `pool_dry` («свежих профилей меньше лимита») вместо молчаливого `done`.
  Smart Connect не затронут (по-прежнему только коннектабельные). Убран мёртвый `HARVEST_PEOPLE`.

## [0.6.0] — 2026-06-27

### Added
- **Просмотр профилей — модуль (People-пиллар).** Заходит на N целевых профилей/день из people-search
  внутри «Запустить» (дефолт 40/день, переиспользует таргет Smart Connect, тот же анти-бан гейт:
  day-keyed cap + jitter + human pace + seen-set dedup + ready-gate). Подтверждённый research рычаг
  (LinkedIn Help a105145: исходящие просмотры растят «Find the right people»). Карточка в «Модули» +
  split в «Отчётах» + список просмотренных.
- **Аудит профиля (Brand-пиллар) — экран + чистая логика.** `auditProfile()`: official All-Star 7 полей
  (hard, гейтят completeness %) + best-practice усиления (soft, честно помечены как НЕ official).
  Экран построен, но **работает на ДЕМО-данных — реальное чтение профиля отложено отдельным шагом,
  вход с Dash скрыт до готовности** (наивный ридер на живом профиле давал ложные «у тебя нет X»;
  нужен честный статус «не смог проверить» + обработка ротации хеша voyager-метода).

### Changed
- Идеи для контента сортируются новые→старые (свежие сверху).
- `weeklyGoal`: People-пиллар → `profile_views` (research: People = просмотры, не коннекты); честная
  строка relationships (bare-инвайты, без «персонального Note»).

### Removed
- Модуль-заглушка `auto_apply` (Фаза 4 отложена).

## [0.5.0] — 2026-06-26

> Ретроспективная сверка версии с реальностью: проект стоял на `0.1.0` с самого init, хотя
> зашипил 5 живых фич-срезов. Версии ниже — приблизительная реконструкция уже сделанного.

### Added
- **Content Pipeline v2** — approve-gate на черновики («Одобрить»/«Отозвать» → `Draft.approved`),
  авто-публикация одобренных как шаг «Запустить» (`publishDays` Пн/Ср/Пт, weekly cap, one/run).
- **ONE-BUTTON consolidation** — единый конфиг-хаб «Модули» + одна «Запустить»↔«Остановить»,
  прогоняющая все включённые модули в безопасном темпе.
- **UX-проход** по всему расширению — per-action фидбэк кнопок, press-affordance,
  «сохранено ✓»/«Скопировано ✓», live-countdown на activity-pill.

### Notes
- Версия теперь честно отражает объём готового. Дальше — по политике бампа.

## [0.4.0] — Content Pipeline v1
- Идеи из ленты → черновики постов; BYOK LLM-слой (`LlmProvider`: OpenRouter + Gemini).

## [0.3.0] — Smart Connect
- People-search → bare-инвайты (multi-region geoUrn), weekly+day-keyed бюджет, sent-set,
  human-pace. Поднимает people + relationships.

## [0.2.0] — Engagement
- Broad-лайки + auto-scroll harvest; judged-комментарии (off-by-default); daily budget +
  human delay; gate (manual/guardrails/full) + quarantine.

## [0.1.0] — Phase 1 SSI engine
- Чтение SSI: internal API (`/sales-api/salesApiSsi`) primary + DOM-парсер `/sales/ssi` fallback;
  фоновый refresh; weekly-goal (слабейший пиллар → рычаг).
